# 把這個檔案複製一份改名成 .env,填入你目前 ngrok 顯示的網址
# 每次重開 ngrok,網址都會變,記得跟著更新這裡
# 這個變數只有本機 `npm run dev`(Vite proxy)會用到
VITE_API_TARGET=https://4edf-2001-b011-3817-1cf0-941d-8798-9d74-a751.ngrok-free.app

# 部署到 Vercel 時,同樣的 ngrok 網址要另外設成 Vercel 專案的
# Environment Variable(在 Vercel dashboard 設定,不要寫進這個檔案),
# 給 api/redfish/[...path].js 這支 Serverless Function 用。
# 注意:不能加 VITE_ 前綴,否則會被打包進前端 bundle 讓瀏覽器看到。
# API_TARGET=https://xxxx.ngrok-free.app
